parameters:
  kapitan:
    compile:
      - input_paths:
          - openshift4-registry/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - openshift4-registry/component/main.jsonnet
        input_type: jsonnet
        output_path: openshift4-registry/
